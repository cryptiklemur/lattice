# Lattice Full App Audit — 2026-03-23

## Bugs

| # | Issue | Location | Status |
|---|-------|----------|--------|
| B1 | Session activation `Promise.all` has no `.catch()` — client hangs if any promise rejects | `server/src/handlers/session.ts:73` | DONE |
| B2 | WebSocket JSON parse errors silently swallowed (empty catch block) | `client/src/providers/WebSocketProvider.tsx:83` | DONE |
| B3 | Context window percent calculation doesn't handle `contextWindow === 0` (returns Infinity) | `client/src/components/chat/ChatView.tsx` | DONE |
| B4 | `require()` in WS router hot path instead of static imports | `server/src/ws/router.ts:76-93` | DONE |
| B5 | Preview cache grows unbounded — never evicted | `client/src/components/sidebar/SessionList.tsx` | DONE |
| B6 | Incomplete cleanup on client disconnect (activeProject, activeSession, pending permissions) | `server/src/daemon.ts:306-315` | DONE |
| B7 | Race condition: multiple concurrent streams for same session (check-then-set) | `server/src/project/sdk-bridge.ts:342` | DONE |
| B8 | Unhandled promise rejection in session history loading | `server/src/handlers/session.ts:73-113` | DONE |

## Security

| # | Issue | Location | Severity | Status |
|---|-------|----------|----------|--------|
| S1 | Auth tokens never expire — persist for daemon lifetime | `server/src/auth/passphrase.ts` | High | DONE |
| S2 | No attachment size limit — large upload can OOM server | `server/src/handlers/attachment.ts:111` | High | DONE |
| S3 | Mesh pairing tokens never expire | `server/src/mesh/pairing.ts:88` | Medium | DONE |
| S4 | `cd` commands auto-approved with no directory boundary check | `server/src/project/sdk-bridge.ts:442` | Medium | DONE |
| S5 | No rate limiting on WebSocket messages | `server/src/daemon.ts` | Medium | DONE |

## Graceful Fallbacks & Retries

| # | Issue | Location | Status |
|---|-------|----------|--------|
| G1 | Session activation needs retry on failure with user feedback | `server/src/handlers/session.ts` + client | DONE |
| G2 | Mesh connections have no timeout — can hang indefinitely | `server/src/mesh/connector.ts:74` | DONE |
| G3 | No graceful server shutdown — active streams lost on restart | `server/src/daemon.ts` | DONE |
| G4 | WebSocket reconnection has no manual retry button | `client/src/providers/WebSocketProvider.tsx` | DONE |
| G5 | No circuit breaker on mesh peer connections | `server/src/mesh/connector.ts` | DONE |

## Feature Ideas (Future)

1. Session search with filters (date, cost, model, message count)
2. Keyboard shortcuts overlay (`?` key)
3. Session cost budget/alerts
4. Split-pane chat view
5. Message bookmarks/pins
6. Session auto-titling after first exchange
7. Connection status indicator in header
8. Relative time auto-refresh (60s interval)
9. Toast duration control (persistent option for errors)
10. Lazy-load settings sections
11. Focus traps on modals
12. `prefers-reduced-motion` guard for inline animations
13. Structured logging (replace console.log/error)
14. HistoryMessage discriminated union type
15. Test suite (WebSocket protocol + session lifecycle)
16. CI pipeline (type-check + build)
